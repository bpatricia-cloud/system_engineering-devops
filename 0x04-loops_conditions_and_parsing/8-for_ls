#!/usr/bin/env bash
# This script lists all visible files in the current directory and prints only the part after the first dash in their names

for file in *; do
  name="${file#*-}"
  echo "$name"
done
